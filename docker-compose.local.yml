services:
  wbw-app:
    build: .
    container_name: wbw-app
    restart: "no"
    environment:
      APP_BASE_URL: "http://localhost:8001"
      DATABASE_URL: "sqlite+aiosqlite:////data/event.db"
      ADMIN_PASSWORD: "admin"
    volumes:
      - ./data:/data
    ports:
      - "8001:8001"
